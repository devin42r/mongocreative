extends ../_bootstrap
append styles
	//	!-- Custom styles for this template --
	link(href="css/cover.css", rel="stylesheet")

block body
	.site-wrapper
		.site-wrapper-inner
			.cover-container
				.masthead.clearfix
					.inner
						h3.masthead-brand Mongo 
						nav
							ul.nav.masthead-nav
								li 
									a(href="/newuser") Add 
								li.active
									a(href="/userlist") View All
				.inner.cover
						block content
						script.
							var something = function() {
								window.location.assign('/newuser')
							}
						h1.
							User List
						span.
							(Click user to send email)
						ul
						each user, i in userlist
							li
								a(href="mailto:#{user.email}")= user.username
								form(method="post" action="deleteUser")
									button(name="username" class="btn btn-sm btn-default" value="#{user.username}") Delete
					//p.lead Programmer: A person who fixed a problem that you don't know you have, in a way you don't understand
					p.lead
					//a(href="#", class="btn btn-lg btn-default") Learn more

				.mastfoot
					.inner
						p By Gary and Devin.
